package postAllProduct;

import java.io.FileInputStream;
import java.io.IOException;
import java.util.Properties;

public class Reusable {
/*
    public static void main(String[] args) throws IOException {
        System.out.println("url from return "+postUrl());
    }
    public static Properties getProperties() throws IOException {

        Properties pro = new Properties();
        String projectPath = System.getProperty("user.dir");
        FileInputStream inputStream = new FileInputStream(projectPath + "/Config.properties");
        pro.load(inputStream);

        return pro;
    }

    public static String postUrl() throws IOException {

        return getProperties().getProperty("baseURL");
    }

 */


     /*public PostProduct() throws IOException {
        {
            UrlReader urlReader = new UrlReader();
            String endPoint = urlReader.postUrl();
            finalUrl = commonBaseUrl.concat(endPoint);
        }*/


  /*  public static void main(String[] args) throws IOException {
        String commonBaseUrl = "https://automationexercise.com/api/";
        UrlReader urlReader = new UrlReader();
        String endPoint=urlReader.postUrl();
        String finalUrl =commonBaseUrl.concat(endPoint);
        System.out.println("The final url is  "+ finalUrl);
        System.out.println(giveUrl());
    }   */

}
